[tox]
env_list = standalone

[testenv:standalone]
description = Build standalone .pyz zipapps (pass target after --, e.g. tox -e standalone -- native)
deps =
    shiv
    build
commands =
    python -m pypi_lockdown._build_standalone {posargs:all}
